
{
  "entities": {
    "User": {
      "title": "User",
      "type": "object",
      "description": "Represents an authenticated admin user.",
      "properties": {
        "id": { "type": "string", "description": "Firebase Authentication UID." },
        "email": { "type": "string", "format": "email" },
        "name": { "type": "string" },
        "role": { "type": "string", "enum": ["admin", "doctor"] }
      },
      "required": ["id", "email", "name", "role"]
    },
    "Patient": {
      "title": "Patient",
      "type": "object",
      "description": "Represents a patient in the system.",
      "properties": {
        "expediente": { "type": "string" },
        "name": { "type": "string" },
        "paternalLastName": { "type": "string" },
        "maternalLastName": { "type": "string" },
        "birthDate": { "type": "string", "format": "date" },
        "age": { "type": "number" },
        "sex": { "type": "string", "enum": ["Hombre", "Mujer"] },
        "birthState": { "type": "string" },
        "address": { "type": "string" },
        "coloniaName": { "type": "string" },
        "phoneNumber": { "type": "string" },
        "curp": { "type": "string" },
        "status": { "type": "string", "enum": ["Vigente", "Baja", "Baja Definitiva"] }
      },
      "required": ["curp", "name", "paternalLastName", "maternalLastName", "phoneNumber"]
    },
    "ServiceType": {
      "title": "Tipo de Consulta",
      "type": "object",
      "description": "Categoría general de atención médica (ej. Consulta Externa, Psicología).",
      "properties": {
        "id": { "type": "string" },
        "name": { "type": "string" },
        "available": { "type": "boolean" }
      },
      "required": ["id", "name", "available"]
    },
    "Specialty": {
      "title": "Especialidad",
      "type": "object",
      "description": "Especialidad médica específica para distinguir entre especialistas.",
      "properties": {
        "id": { "type": "string" },
        "name": { "type": "string" },
        "available": { "type": "boolean" }
      },
      "required": ["id", "name", "available"]
    },
    "ClinicBlock": {
      "title": "Bloqueo de Agenda",
      "type": "object",
      "description": "Días de vacaciones o cierres especiales de un consultorio.",
      "properties": {
        "id": { "type": "string", "description": "clinicId_date" },
        "clinicId": { "type": "string" },
        "date": { "type": "string", "format": "date" },
        "type": { "type": "string", "enum": ["vacation", "custom"] },
        "endTime": { "type": "string", "description": "Hora de cierre especial" },
        "reason": { "type": "string" }
      }
    }
  },
  "firestore": {
    "/serviceTypes/{id}": {
      "schema": "#/entities/ServiceType",
      "description": "Catálogo de tipos de consulta general."
    },
    "/specialties/{id}": {
      "schema": "#/entities/Specialty",
      "description": "Catálogo de especialidades médicas para especialistas."
    },
    "/clinicBlocks/{id}": {
      "schema": "#/entities/ClinicBlock",
      "description": "Tabla independiente para vacaciones y horarios especiales de unidades."
    }
  },
  "storage": {
    "description": "Almacenamiento de logotipos institucionales y recursos multimedia.",
    "rules": "allow read: if true; allow write: if request.auth != null;"
  }
}
